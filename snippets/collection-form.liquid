{% if product.price < 10000 %}
  <a href="{{ product.url }}">
    <img src="{{ product | image_url }}">
    <p>{{ product.title }}</p>
  </a>
  {% form "product", product %}
  {% endform %}
{% endif %}
